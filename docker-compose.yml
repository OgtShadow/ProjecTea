version: "3.9"
services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "8080:80"

  go-backend:
    build:
      context: ./server/go
      dockerfile: Dockerfile
    ports:
      - "8081:8080"

  java-backend:
    build:
      context: ./server/java
      dockerfile: Dockerfile
    ports:
      - "8082:8080"
