meta {
  name: Post message
  type: http
  seq: 4
}

post {
  url: {{JAVA_API_URL}}/api/messages
  body: json
  auth: inherit
}

body:json {
  {
    "text": "woof woof bark bark"
  }
}

tests {
  test("message post returns 401 when NOT authenticated", async function () {
    expect(res.status).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
