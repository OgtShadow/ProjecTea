meta {
  name: Get messages
  type: http
  seq: 2
}

get {
  url: https://localhost/api/messages
  body: none
  auth: inherit
}

tests {
  test("Messages endpoint returns 200 when NOT authenticated", async function () {
    expect(res.status).to.equal(200);
    expect(Array.isArray(res.body)).to.equal(true);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
