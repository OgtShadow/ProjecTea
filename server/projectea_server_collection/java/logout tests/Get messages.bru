meta {
  name: Get messages
  type: http
  seq: 2
}

get {
  url: https://localhost/api/messages
  body: none
  auth: inherit
}

tests {
  test("Messages endpoint returns 401 when NOT authenticated", async function () {
    expect(res.status).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
