meta {
  name: Untitled
  type: http
  seq: 1
}

post {
  url: {{JAVA_API_URL}}/api/auth/logout
  body: none
  auth: inherit
}

tests {
  test("Logout returns 401 || 204", async function () {
    expect(res.status).to.be.oneOf([401, 204]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
