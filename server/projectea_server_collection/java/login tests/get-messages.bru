meta {
  name: Get messages
  type: http
  seq: 3
}

get {
  url: https://localhost/api/messages
  body: none
  auth: none
}

tests {
  test("Messages endpoint returns 200 when authenticated", async function () {
    expect(res.status).to.equal(200);
  });
}
