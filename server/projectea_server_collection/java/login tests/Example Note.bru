meta {
  name: Example Note
  type: http
  seq: 3
}

post {
  url: {{JAVA_DIRECT_URL}}/api/notes
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Example Note",
    "content": "Testing MongoDB Database."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
