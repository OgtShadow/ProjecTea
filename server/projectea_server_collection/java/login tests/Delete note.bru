meta {
  name: Delete note
  type: http
  seq: 11
}

delete {
  url: https://localhost/api/notes/{{noteId}}
  body: none
  auth: inherit
}

headers {
  Cookie: {{sessionCookie}}
}

tests {
  test("delete note returns 204", async function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
