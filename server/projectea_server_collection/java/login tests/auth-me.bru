meta {
  name: Auth me (check current user)
  type: http
  seq: 2
}

get {
  url: {{JAVA_API_URL}}/api/auth/me
  body: none
  auth: none
}

tests {
  test("Auth me returns 200 when authenticated", async function () {
    expect(res.status).to.equal(200);
  });
  
  test("Auth me returns name when authenticated", async function () {
    expect(res.body.username).to.equal("OGT_SHADOW");
  });
  
}
