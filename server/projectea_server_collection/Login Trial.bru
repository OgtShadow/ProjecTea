meta {
  name: Login Trial
  type: http
  seq: 3
}

post {
  url: {{JAVA_API_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "OGT_SHADOW",
    "password": "DwukropekTrzy"
  }
}

tests {
  test("Login trial returns 200 and username", async function () {
    expect(res.status).to.equal(200);
    expect(res.body.username).to.equal("OGT_SHADOW");
  });
}

settings {
  encodeUrl: true
}
