meta {
  name: Get messages
  type: http
  seq: 2
}

get {
  url: https://localhost:8082/api/messages
  body: none
  auth: none
}

tests {
  test("Messages endpoint returns 200", async function () {
    expect(res.status).to.equal(200);
  });
}
