meta {
  name: Go Api life status
  type: http
  seq: 1
}

get {
  url: http://localhost:8081/health
  body: none
  auth: none
}

tests {
  test("Server is on", async function () {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("healthy");
  });
  
}
